# Use after the production IP certificate has been issued.
services:
  api-prod:
    environment:
      APP_CORS_ALLOWED_ORIGINS: https://elianecarneiroimoveis.com.br,https://www.elianecarneiroimoveis.com.br,https://54.94.105.56,http://localhost:8081,http://127.0.0.1:8081
  public-ip:
    ports:
      - "443:443"
    volumes:
      - ./nginx-ip-https.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
