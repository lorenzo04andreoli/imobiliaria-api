# Use after the production IP certificate has been issued.
services:
  public-ip:
    ports:
      - "443:443"
    volumes:
      - ./nginx-ip-https.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
