services:
  mysql:
    image: mysql:8.4
    container_name: imobiliaria-mysql
    restart: unless-stopped
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD nao configurado}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-imobiliaria_db}
      MYSQL_USER: ${MYSQL_USER:-imobiliaria_user}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?MYSQL_PASSWORD nao configurado}
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mysql_data:
